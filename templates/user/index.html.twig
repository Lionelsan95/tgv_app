{% extends 'base.html.twig' %}

{% block title %}Home{% endblock %}

{% block body %}

    <div class="row">
        <div class="col-xs-12 col-xl-8 offset-xl-1 w-100 sup-frame p-5">
            <div class="d-flex justify-content-between align-items-center mb-5 color-first-color">
                <h1 class="color-third-color"><i class="fas fa-hashtag color-third-color"></i> Users list </h1>
                <div class="d-flex">
                    <a href="{{ path('admin_user_new') }}" class="btn color-first-color d-flex justify-content-between align-items-center">
                        <span class="fa-stack" style="vertical-align: top;">
                          <i class="fas fa-circle fa-stack-2x"></i>
                          <i class="fas fa-plus fa-stack-1x fa-inverse"></i>
                        </span>
                        Create account
                    </a>
                </div>
            </div>

            <div class="inf-frame">
                <div class="table-responsive">
                    <!--<table class="table">
                        <thead>
                        <tr>
                            <th>#</th>
                            <th>First name</th>
                            <th>Last name</th>
                            <th>Email</th>
                            <th class="text-center">Balance</th>
                            <th class="text-center">Actions</th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for user in users %}
                            <tr>
                                <td class="color-fourth-color">
                                    <a href="{{ path('user_show', {'id': user.id}) }}">{{ loop.index }}</a>
                                </td>
                                <td>{{ user.firstName }}</td>
                                <td>{{ user.lastName }}</td>
                                <td>{{ user.email }}</td>
                                <td class="text-center">{{ user.solde }}</td>
                                <td class="text-center color-fourth-color">
                                    <a href="{{ path('user_edit', {'id': user.id}) }}">
                                        <i class="fas fa-pen"></i>
                                    </a>
                                </td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                    <hr>-->
                    <input type="hidden" id="url" value="{{ path('user_list') }}"/>
                    <table class="table" id="userList">
                        <thead>
                        <tr>
                            <th>#</th>
                            <th>First name</th>
                            <th>Last name</th>
                            <th>Email</th>
                            <th class="text-center">Balance</th>
                            <th class="text-center">Actions</th>
                        </tr>
                        </thead>
                        <tbody>
                        </tbody>
                    </table>
                </div>
            </div>

        </div>
    </div>

{% endblock %}
{% block javascripts %}
    {{ parent() }}
    <script src="https://unpkg.com/bootstrap-table@1.14.2/dist/bootstrap-table.min.js"></script>
    <script src="{{ asset('js/utils.js') }}"></script>
{% endblock %}